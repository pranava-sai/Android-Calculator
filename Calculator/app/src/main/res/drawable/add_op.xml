<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- This shape represents a rounded rectangle with an orange fill and a white stroke. -->
    <solid android:color="@color/orange"/>
    <stroke android:width="1dp" android:height="100dp" android:color="@color/white"/>
    <corners android:radius="1000dp"/>
</shape>
